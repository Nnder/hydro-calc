// ~/composables/useXmlData.js
export const useXmlData = processedData => {
  // Теперь processedData — это уже готовый объект { sections, categoryMap, allOffers }
  // Никакого парсинга! Просто возвращаем
  return processedData
}
